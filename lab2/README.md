
---

```markdown
# ЛР2 — Визуализация и связь признаков (clients2.csv)

**Файл:** `ЛР2_Кольцов_13.ipynb`  
**Вариант:** 13  
**Датасет:** `clients2.csv`

## Цель работы
Изучить связь между признаками двумерного (и многомерного) набора данных и выполнить визуализацию данных.

## Что сделано в ноутбуке
- Загрузка и первичный анализ датасета
- Базовая визуализация распределений:
  - гистограммы, boxplot
  - анализ выбросов и разброса
- Анализ взаимосвязей признаков:
  - диаграммы рассеяния
  - корреляционная матрица
  - тепловая карта корреляций (`seaborn.heatmap`)
  - `pairplot` для набора числовых признаков
- Индивидуальные задания:
  1) **Seaborn:** группировка по `education` и `marital_status` + визуализация (histplot)  
  2) **Pandas + plot:** сводная таблица среднего дохода по семейному статусу + scatter  
  3) **Matplotlib:** фильтр клиентов (например по `children_count > 0`) + круговая диаграмма распределения образования
- Продвинутая визуализация:
  - `hexbin` график зависимости (например покупок от дохода)

## Как запустить
1. Положи `clients2.csv` в проект (например `data/clients2.csv`)
2. В ноутбуке замени путь чтения на относительный:
   ```python
   df = pd.read_csv("data/clients2.csv")
