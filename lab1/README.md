
---

```markdown
# ЛР1 — Предобработка данных (movies.csv)

**Файл:** `ЛР1_Кольцов_13.ipynb`  
**Вариант:** 13  
**Датасет:** `movies.csv`

## Цель работы
Осуществить предварительную обработку данных CSV-файла: выявить проблемы качества данных и устранить их.

## Что сделано в ноутбуке
- Загрузка датасета `movies.csv` (учёт разделителя `;`)
- Первичный обзор:
  - `head()`, `info()`, `describe()`
  - проверка типов данных
- Проверка качества данных:
  - пропуски (`isna`, подсчёт строк с пропусками)
  - удаление строк с пропусками (по решению в работе)
  - поиск явных дубликатов
- Работа с признаками и аналитика:
  - проверка и корректировка типов
  - расчёты/группировки по жанрам, годам и рейтингу
  - создание категориальных признаков:
    - **категория года** (диапазоны по заданию)
    - **категория рейтинга** (по перцентилям)
  - TOP-5 жанров по популярности
  - фильтрации (`query`)
  - сводные таблицы (`pivot_table`) и выводы

## Как запустить
1. Положи `movies.csv` в папку проекта (например `data/movies.csv`)
2. В ноутбуке замени путь чтения на относительный:
   ```python
   df = pd.read_csv("data/movies.csv", sep=";")
